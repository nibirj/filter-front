class FilterWithCriteriaDTO {
    constructor(id, name, criteriaDTOList) {
        this.id = id;
        this.name = name;
        this.criteriaDTOList = criteriaDTOList;
    }
}

export { FilterWithCriteriaDTO }